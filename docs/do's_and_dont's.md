to with the dynamic keys that the dynamic key is rotated every once in a while, and the end isn't constantly being updated with new values. And the dynamic key constantly gains a new value, so it needs that update. and the database is the only thing that can hold it without reqiring a human to intervene evrytime it changes 


befiore changing direction we should ask if we do use env then who ius goiung to be responsible for watching whenever the quick books token gets rotated, and who is resposible for updating the file when the old one stops workingn 